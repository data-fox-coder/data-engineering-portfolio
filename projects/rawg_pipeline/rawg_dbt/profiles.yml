rawg_dbt:
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', '../rawg_data.duckdb') }}"
      schema: main
      threads: 1
    prod:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', '../rawg_data.duckdb') }}"
      schema: main
      threads: 4